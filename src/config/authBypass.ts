/**
 * TEMPORARY TESTING MODE
 * When true, all auth/role route guards are bypassed so every page
 * can be opened without logging in. Set to false to re-enable login.
 */
export const AUTH_BYPASS = true;
